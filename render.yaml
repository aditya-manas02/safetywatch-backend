services:
  - type: web
    name: safetywatch-backend
    runtime: node
    buildCommand: npm cache clean --force && npm ci
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.19.5
